package cmd

import (
	"os"

	"github.com/spf13/cobra"
)

var (
	Version = "v0.1.0"
	Date    = "2023-04-03"
)

var rootCmd = &cobra.Command{
	Use:   "escrow-account-signature",
	Short: "escrow-account-signature",
	Run: func(cmd *cobra.Command, args []string) {
		cmd.Help()
	},
}

func Execute() {
	if err := rootCmd.Execute(); err != nil {
		os.Exit(-1)
	}
}
